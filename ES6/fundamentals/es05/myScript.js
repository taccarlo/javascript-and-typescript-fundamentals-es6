const age=40;
//const a = "hello Mario"+(1+1)+age;
//alt + 96
const a = `hello Mario ${1+1} (${age})`;
console.log(a);

//con backtick ` possibilità di fare stringa multiline

//const url = `${API}/${endpoint}/${id}`;

